Legacy Config Examples
======================

These files are retained only as examples of the older workflow shape. Active
science runs should use ``configs/fs2_phz1_science.yaml``.

Legacy configs may include assumptions that are no longer active, such as
photo-z column initialization or older parameter choices. Do not use them for
new Euclid + LSST science runs without updating them to the current workflow.
