extends: feniks_joint_prior_ind_joint.yaml

amortized:
  prior:
    update_schedule: variational_em
    update_every_epochs: 4
    mstep_only: true
  training:
    # 120 encoder epochs plus 30 prior-only M-steps.
    epochs: 150
