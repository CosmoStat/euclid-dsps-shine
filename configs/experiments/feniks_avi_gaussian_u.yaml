extends: feniks_conditional_posterior_common.yaml
amortized:
  objective: {mode: stochastic_elbo}
  encoder: {type: prior_transport_gaussian}
  training: {epochs: 120, learning_rate: 1.0e-4, kl_annealing_epochs: 40, kl_weight_max: 1.0}
